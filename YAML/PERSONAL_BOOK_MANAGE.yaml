!!yukimura1227.generate.sql.map.util.TableInfoBean
tableName : PERSONAL_BOOK_MANAGE
columnList:
- {
    columnName : id,
    columnType : varchar,
    javaType   : String,
  }
- {
    columnName : isbn,
    columnType : varchar,
    javaType   : String,
  }
- {
    columnName : status_id,
    columnType : char,
    javaType   : String,
  }
- {
    columnName : remark,
    columnType : remark,
    javaType   : String,
  }
- {
    columnName : bought_date,
    columnType : date,
    javaType   : String,
  }
