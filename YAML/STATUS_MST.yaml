!!yukimura1227.generate.sql.map.bean.TableInfoBean
tableName : STATUS_MST
columnList:
- {
    columnName : status_id,
    columnType : char,
    javaType   : String,
  }
- {
    columnName : status_name,
    columnType : varchar,
    javaType   : String,
  }
