!!yukimura1227.generate.sql.map.bean.TableInfoBean
tableName : LOGIN_USER
columnList:
- {
    columnName : id,
    columnType : varchar,
    javaType   : String,
  }
- {
    columnName : password,
    columnType : varchar,
    javaType   : String,
  }
