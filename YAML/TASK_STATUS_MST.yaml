!!yukimura1227.generate.sql.map.util.TableInfoBean
tableName : TASK_STATUS_MST
columnList:
- {
    columnName : status_id,
    columnType : char,
    javaType   : String,
  }
- {
    columnName : status_name,
    columnType : varchar,
    javaType   : String,
  }
